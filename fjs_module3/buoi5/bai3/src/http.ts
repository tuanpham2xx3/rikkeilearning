import axios from'axios';let accessToken:string|null='demo-access-token';export const setToken=(value:string|null)=>{accessToken=value};export const api=axios.create({baseURL:'http://localhost:3004'});api.interceptors.request.use(config=>{if(accessToken)config.headers.Authorization=`Bearer ${accessToken}`;return config});
