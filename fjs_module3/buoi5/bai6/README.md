# Bài 6 — Resilient API Client
Module bao phủ timeout 5s, Bearer token, trả `response.data`, 401/500, chuẩn hóa `get/post/put/remove` và loại params `undefined/null` trước khi gửi Axios. UI không chứa logic mạng.
