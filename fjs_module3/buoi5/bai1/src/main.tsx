import React,{useEffect,useState}from'react';import ReactDOM from'react-dom/client';import{addContact,deleteContact,getContacts,type Contact}from'./api';import'./styles.css';function App(){const[c,setC]=useState<Contact[]>([]);const[msg,setMsg]=useState('');const load=()=>getContacts().then(r=>setC(r.data)).catch(()=>setMsg('Hãy chạy npm run server'));useEffect(()=>{void load()},[]);return <main><h1>Danh bạ</h1><button onClick={async()=>{await addContact({name:'Số mới',phone:'0900000000'});void load()}}>Thêm số</button>{msg&&<p>{msg}</p>}{c.map(x=><p key={x.id}>{x.name} - {x.phone}<button onClick={async()=>{try{await deleteContact(x.id);setMsg('Xóa thành công');void load()}catch(e){setMsg(e instanceof Error?e.message:'Lỗi')}}}>Xóa</button></p>)}</main>}ReactDOM.createRoot(document.getElementById('root')!).render(<App/>);
