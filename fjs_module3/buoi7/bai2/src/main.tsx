import React,{useState}from'react';import{createRoot}from'react-dom/client';import{useDebounce}from'./useDebounce';function App(){const[q,setQ]=useState('');const d=useDebounce(q);return <main><h1>Autocomplete chống spam</h1><input onChange={e=>setQ(e.target.value)} placeholder="Gõ nhanh..."/><p>Chỉ gọi API sau 400ms không gõ. Keyword: {d}</p></main>}createRoot(document.querySelector('#root')!).render(<App/>);
