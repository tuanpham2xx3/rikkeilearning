import{configureStore,createSlice}from'@reduxjs/toolkit';import type{PayloadAction}from'@reduxjs/toolkit';type Cart={id:number;name:string;price:number;qty:number};const cart=createSlice({name:'cart',initialState:[] as Cart[],reducers:{add:(s,a:PayloadAction<Cart>)=>{s.push(a.payload)},clear:()=>[]}});const address=createSlice({name:'address',initialState:'',reducers:{setAddress:(_,a:PayloadAction<string>)=>a.payload}});export const store=configureStore({reducer:{cart:cart.reducer,address:address.reducer}});export const{add,clear}=cart.actions;export const{setAddress}=address.actions;
