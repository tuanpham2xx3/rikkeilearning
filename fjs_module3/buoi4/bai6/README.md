# Bài 6 — Luồng chấm điểm
`Nhập sĩ số` → kiểm tra số nguyên dương → `useEffect` tạo số phần tử `scores` tương ứng → render ô điểm → Yup resolver kiểm tra mã môn/in hoa và từng điểm 0–10 → submit. Sĩ số không hợp lệ thì mảng điểm rỗng và hiện lỗi.
