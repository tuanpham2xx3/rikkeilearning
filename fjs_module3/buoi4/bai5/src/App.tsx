import {useFieldArray,useForm}from'react-hook-form';type Data={items:{name:string;price:number}[]};function App(){const{register,control,handleSubmit,formState:{errors}}=useForm<Data>({defaultValues:{items:[]}});const{fields,append,remove}=useFieldArray({control,name:'items',rules:{minLength:{value:1,message:'Cần ít nhất một món đồ'}}});return <main><h1>Lập kế hoạch chi tiêu</h1><form onSubmit={handleSubmit(v=>alert(JSON.stringify(v,null,2)))}>{fields.map((f,i)=><div className="item" key={f.id}><input placeholder="Tên món đồ" {...register(`items.${i}.name` as const,{required:true})}/><input type="number" placeholder="Giá tiền" {...register(`items.${i}.price` as const,{valueAsNumber:true,min:0})}/><button type="button" onClick={()=>remove(i)}>Xóa</button></div>)}{errors.items?.root&&<small>{errors.items.root.message}</small>}<button type="button" onClick={()=>append({name:'',price:0})}>Thêm món đồ</button><button>Gửi kế hoạch</button></form></main>}export default App;
