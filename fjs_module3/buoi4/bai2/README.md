# Bài 2 — Uncontrolled với React Hook Form
Không truyền `value`/`onChange` cho input. `register()` gắn ref vào DOM; React Hook Form lấy giá trị khi cần, giảm re-render với textarea lớn. Validation native của RHF được khai báo trực tiếp trong `register`.
