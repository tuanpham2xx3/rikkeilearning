import { useForm } from 'react-hook-form'; type FormData={title:string;content:string};
function App(){const {register,handleSubmit,formState:{errors}}=useForm<FormData>();return <main><h1>Soạn bài viết Blog</h1><p>React Hook Form dùng uncontrolled inputs: DOM giữ giá trị, thư viện đọc qua ref nên không re-render cả form mỗi lần gõ.</p><form onSubmit={handleSubmit((data)=>alert(JSON.stringify(data,null,2)))}><label>Tiêu đề<input {...register('title',{required:'Vui lòng nhập tiêu đề'})}/>{errors.title&&<small>{errors.title.message}</small>}</label><label>Nội dung<textarea rows={10} {...register('content',{required:'Nội dung là bắt buộc',minLength:{value:50,message:'Nội dung tối thiểu 50 ký tự'}})}/>{errors.content&&<small>{errors.content.message}</small>}</label><button>Đăng bài</button></form></main>}export default App;
