# Bài 2 — Build
Chạy `npm run build` để tạo `dist/`. Development phục vụ module chưa tối ưu; production bundle được minify và tên file có hash để cache browser tự nhận biết file mới khi deploy, tránh dùng bundle cũ.
