# Bài 6 — CI/CD
Vercel Build Command đặt là `npm run vercel-build`, nên test fail sẽ dừng trước build/deploy. GitHub Actions cũng chạy chuỗi `npm ci → test → build`. Tạo repo, kết nối Vercel để lấy Live URL và ảnh build logs; đây là thao tác cần tài khoản GitHub/Vercel của bạn.
